<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <title>Edit - <%= filename %>
  </title>
  <style>
    body {
      font-family: 'Outfit', sans-serif;
    }
  </style>
</head>

<body
  class="bg-zinc-900 text-zinc-100 min-h-screen selection:bg-blue-500 selection:text-white flex items-center justify-center">

  <div class="w-full max-w-lg p-6">
    <div class="bg-zinc-800/50 backdrop-blur-xl border border-zinc-700/50 p-10 rounded-2xl shadow-2xl">

      <div class="mb-8">
        <a class="text-zinc-500 hover:text-white text-sm font-medium transition-colors" href="/">&larr; Cancel and go
          back</a>
        <h1 class="text-2xl font-bold mt-4">Rename Note</h1>
        <p class="text-zinc-400 text-sm mt-1">Give your note a fresh new title.</p>
      </div>

      <form action="/edit" method="post" class="space-y-4">
        <div>
          <label class="block text-xs font-semibold text-zinc-500 uppercase tracking-wider mb-2">Previous Name</label>
          <input
            class="w-full bg-zinc-900/50 border border-zinc-700 rounded-xl px-5 py-3 text-zinc-400 cursor-not-allowed"
            type="text" name="prev" value="<%= filename %>" readonly />
        </div>

        <div>
          <label class="block text-xs font-semibold text-zinc-400 uppercase tracking-wider mb-2">New Name</label>
          <input
            class="w-full bg-zinc-700/20 border border-zinc-600 rounded-xl px-5 py-3 text-white placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-yellow-500/50 focus:border-yellow-500 transition-all font-medium"
            type="text" name="new" placeholder="Enter new name" autofocus />
        </div>

        <button
          class="w-full mt-2 bg-yellow-600 hover:bg-yellow-500 text-white font-bold py-3 px-4 rounded-xl transition-all shadow-lg shadow-yellow-600/20 active:scale-95"
          type="submit">
          Update Filename
        </button>
      </form>

    </div>
  </div>

</body>

</html>